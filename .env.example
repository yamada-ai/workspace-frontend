# Frontend Environment Variables

# API URL - デフォルトは相対パス（Nginx経由でアクセスする場合）
# 直接アクセス（localhost:3000）の場合は以下を設定:
# VITE_API_URL=http://localhost:8000

# WebSocket URL - デフォルトはwindow.location.hostを使用（Nginx経由）
# 直接アクセス（localhost:3000）の場合は以下を設定:
# VITE_WS_URL=ws://localhost:8000/ws
